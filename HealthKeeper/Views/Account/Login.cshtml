@{
    ViewData["Title"] = "Login";
}
@model ErrorViewModel

<link rel="stylesheet" href="~/css/login.css" />

<div class="divlogin">
    <div class="login-container">
        <form class="login-form" asp-action="Login" asp-controller="Account">
            @Html.AntiForgeryToken()
            <h2 class="center">Login</h2>
            @{
                if (Model.ShowError)
                {
                    <h2>@Model.Error</h2>
                }
            }
            <div class="input-grouplogin">
                <label class="labellogin" for="username">Nutzername:</label>
                <input class="inputlogin" type="text" name="username" required>
            </div>
            <div class="input-grouplogin">
                <label class="labellogin" for="password">Passwort:</label>
                <input class="inputlogin" type="password" name="password" required>
            </div>
            <input class="button" type="submit" value="Login" />
            <a class="a_extra" asp-controller="Account" asp-action="Register">Erstelle einen Account.</a>
        </form>
    </div>
</div>


